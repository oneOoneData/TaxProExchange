// Profile layout - metadata is handled by page.tsx to avoid conflicts
// This layout exists only for structure, not metadata generation

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
